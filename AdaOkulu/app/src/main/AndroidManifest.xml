<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bazz.adaokulu">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.CALL_PHONE"></uses-permission>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

        <!-- Include following permission if you load images from Internet -->
        <uses-permission android:name="android.permission.INTERNET" />
        <!-- Include following permission if you want to cache images on SD card -->
        <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
        ...

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logoapp"
        android:label="ADA OKULU"
        android:supportsRtl="true"

        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".AnasayfaActivity.IletisimActivity"></activity>

        <activity
            android:name=".AnasayfaActivity.EgitimActivity"
            android:theme="@style/AppThemeBildirim"></activity>
        <activity
            android:name=".AnasayfaActivity.MisyonActivity"
            android:theme="@style/AppThemeBildirim"></activity>
        <activity
            android:name=".AnasayfaActivity.VizyonActivity"
            android:theme="@style/AppThemeBildirim"></activity>
        <activity
            android:name=".AnasayfaActivity.GaleriActivity"
            android:theme="@style/AppThemeBildirim"></activity>
        <activity
            android:name=".LoginPage"
            android:theme="@style/AppThemeBildirim"></activity>

        <activity
            android:name=".OBS.OgrenciProfili"
            android:theme="@style/AppThemeBildirim"></activity>
        <activity
            android:name=".AnasayfaActivity.BildirimlerActivity"
            android:theme="@style/AppThemeBildirim"></activity>
        <activity
            android:name=".OBS.SinavSonucActivity"
            android:theme="@style/AppThemeBildirim"></activity>
        <activity
            android:name=".OBS.BeslenmeActivity"
            android:theme="@style/AppThemeBildirim"></activity>
        <activity
            android:name=".OBS.DevamsizlikActivity"
            android:theme="@style/AppThemeBildirim"

            android:windowSoftInputMode="adjustPan"
            android:screenOrientation="portrait"></activity>
        <activity
            android:name=".OBS.SinavTakvimiActivity"
            android:theme="@style/AppThemeBildirim"></activity>
        <activity
            android:name=".OBS.ServisSaatleri"
            android:label="Servis Saatleri"
            android:theme="@style/AppThemeBildirim"></activity>

    </application>

</manifest>